// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  i18n: {
    locales: ['en', 'nl'],
    defaultLocale: 'nl',
    localeDetection: false
  },
}

module.exports = nextConfig
